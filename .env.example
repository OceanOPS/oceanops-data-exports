# Copy to .env and fill in (both export:partners and export:geojson read this file).
# Shell variables override .env. For one-off runs:
#   export OCEANOPS_DATABASE_URL='postgresql://...'
#   npm run export:all

OCEANOPS_DATABASE_URL=postgresql://user:password@host:5432/oceanops
OCEANOPS_EXPORT_EDITION=report-card
